# Real GitHub personal access token. Required: e2e tests hit the live
# GitHub API to validate repositories. Public-repo scope is enough.
#
# Copy this file to .env.e2e and fill in a real token before running
# `make test-e2e`. The .env.e2e file is gitignored.
GITHUB_TOKEN=
