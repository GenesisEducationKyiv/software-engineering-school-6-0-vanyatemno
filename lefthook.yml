pre-commit:
  parallel: true
  jobs:
    - name: lint
      run: make lint
    - name: swagger docs update
      run: make swagger && git add services/api/docs/*
    - name: unit tests
      run: make test-unit